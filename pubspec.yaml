name: metatagger
description: A pure Dart library for reading and writing metadata to MP3, MP4/M4A, FLAC, and OGG files with custom tag support.
version: 2.1.0
repository: https://github.com/bw8686/metatagger

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
